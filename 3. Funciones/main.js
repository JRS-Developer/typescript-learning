// ╔══════════════╗
// 3. FUNCIONES
// ╚══════════════╝
// 3.1 Parametros
// ---------------------------------------
// Para los parametros se hace lo mismo que las variables, se les indica el tipo con:
// EJ: (variable:tipo)
/*
    const getFullname = (name: string, surname: string) => {
        return `${name} ${surname}`;
    };

    console.log(getFullname('Jose', 'Sanchez'));
*/
// 3.2 Especificando que retornara
// ---------------------------------------
// Despues de los parametros de la funcion se coloca el tipo de valor que retornara nuestra funcion
// EJ: const funcion = (parametros):tipo => {}
/*
    const getFullname = (name: string, surname: string): string => {
        return `${name} ${surname}`;
    };

    console.log(getFullname('Jose', 'Sanchez'));
*/
